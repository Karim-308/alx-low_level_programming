#include "main.h"

/**
 * _abs - return 0 the positive of any number 
 *
 * @n: int to be checked
 *  Return: Always 0 (success)
  */
int _abs(int)
{
	if (n == 0)
	{
		return (0);
	}

	else if (n > 0)
	{
		return (n);
	}

	else
	{
		return (-1 * n );
	}

}
